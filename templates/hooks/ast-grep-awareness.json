{
  "id": "ast-grep-awareness",
  "name": "AST-Grep Tool Awareness",
  "description": "Provides context about ast-grep capabilities and suggests when to use it over regular grep",
  "event": "UserPromptSubmit",
  "enabled": true,
  "command": "${HOOK_SCRIPT}",
  "continueOnError": true,
  "priority": 90,
  "requirements": {
    "commands": ["which", "npm", "echo"],
    "env": [],
    "files": []
  }
}